{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Chess",
            "type": "shell",
            "command": "cd ${workspaceFolder}/build && cmake --build . --target demo",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
